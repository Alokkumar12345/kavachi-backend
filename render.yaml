yaml
services:
  - type: web
    name: kavachi-backend
    env: node
    plan: free
    buildCommand: "chmod +x build.sh && ./build.sh"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: FIREBASE_SERVICE_ACCOUNT
        sync: false
